source "https://rubygems.org"

# rubocop pin matches what we run locally + what's documented in
# .rubocop.yml as the version the rules were validated against.
# `~> 1.86` floats within the 1.x line so security/bug-fix releases
# get picked up without breaking the cop set.
gem "rubocop", "~> 1.86"
